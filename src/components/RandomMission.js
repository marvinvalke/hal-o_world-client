import React from "react";

function RandomMission(props) {
  console.log(props);

  const {randomBtn} = props

  return (
    <div>
      <h1>Random</h1>
    </div>
  );
}

export default RandomMission;
